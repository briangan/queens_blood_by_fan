%nav(class="navbar navbar-expand-lg p-0")
  .container-fluid
    %a(class="navbar-brand" href="/" title="Back Home") Queen's Blood by Fan
    %button(class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent")
      %span.navbar-toggler-icon
    %div(class="collapse navbar-collapse" id="navbarSupportedContent")
      %ul(class="navbar-nav me-auto mb-2 mb-lg-0")
        %li.nav-item
          = link_to 'Game', Game.first, class:"nav-link#{' active' if params[:controller] == 'games' }"
        %li.nav-item
          = link_to 'Boards', boards_path, class:"nav-link#{' active' if params[:controller] == 'boards' }"
        %li.nav-item
          = link_to 'Cards', cards_path, class:"nav-link#{' active' if params[:controller] == 'cards' }"
    
    - if user_signed_in?
      .dropdown.float-end
        %a(class="dropdown-toggle text-deco-none" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false")
          %strong #{current_user.username}
        %ul.dropdown-menu.dark-blood-bg-color.subtle-shadow(style="left: -50%;")
          %li 
            = link_to 'Decks', decks_path, class:"dropdown-item"
      &nbsp;
      = link_to ' Logout', destroy_user_session_path, method: :delete, class:'btn bi bi-box-arrow-up-left'
    - else
      .float-end
        = link_to ' Login', new_user_session_path, class:'btn bi bi-box-arrow-in-down-right'
    .clearfix